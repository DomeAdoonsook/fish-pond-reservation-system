<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>เช็ควัสดุคงคลัง - LIFF</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { font-family: 'Sarabun', sans-serif; }
    body {
      background: linear-gradient(135deg, #1abc9c 0%, #16a085 100%);
      min-height: 100vh;
      padding: 15px;
      padding-bottom: 50px;
    }
    .back-btn { color: white; text-decoration: none; display: inline-flex; align-items: center; gap: 5px; margin-bottom: 15px; }
    .page-title { color: white; font-size: 1.5rem; font-weight: 600; margin-bottom: 20px; text-align: center; }
    .content-card { background: white; border-radius: 20px; padding: 20px; box-shadow: 0 10px 40px rgba(0,0,0,0.1); }
    .category-title { font-weight: 600; color: #333; padding: 10px 0; border-bottom: 2px solid #1abc9c; margin-bottom: 10px; }
    .stock-item { display: flex; justify-content: space-between; padding: 12px 0; border-bottom: 1px solid #eee; }
    .stock-item:last-child { border-bottom: none; }
    .item-name { font-weight: 500; color: #333; }
    .item-qty { font-weight: 600; }
    .item-qty.low { color: #e74c3c; }
    .item-qty.medium { color: #f39c12; }
    .item-qty.high { color: #27ae60; }
    .search-box { border-radius: 12px; padding: 12px 15px; border: 2px solid #e0e0e0; margin-bottom: 20px; }
    .search-box:focus { border-color: #1abc9c; box-shadow: none; }
    .filter-btn { border-radius: 20px; margin: 3px; font-size: 0.85rem; }
    .filter-btn.active { background: #1abc9c; border-color: #1abc9c; }
  </style>
</head>
<body>
  <a href="/liff" class="back-btn"><i class="bi bi-arrow-left"></i> กลับ</a>
  <h1 class="page-title"><i class="bi bi-clipboard-data me-2"></i>เช็ควัสดุคงคลัง</h1>

  <div class="content-card">
    <input type="text" class="form-control search-box" id="searchInput" placeholder="🔍 ค้นหาวัสดุ..." onkeyup="filterStock()">

    <div class="mb-3 text-center">
      <button class="btn btn-outline-secondary filter-btn active" data-cat="" onclick="filterCategory(this, '')">ทั้งหมด</button>
      <% categories.forEach(cat => { %>
        <button class="btn btn-outline-secondary filter-btn" data-cat="<%= cat.id %>" onclick="filterCategory(this, '<%= cat.id %>')"><%= cat.name %></button>
      <% }) %>
    </div>

    <div id="stockList">
      <% categories.forEach(cat => { %>
        <% const catItems = items.filter(i => i.category_id === cat.id); %>
        <% if (catItems.length > 0) { %>
          <div class="category-section" data-category="<%= cat.id %>">
            <div class="category-title"><%= cat.name %></div>
            <% catItems.forEach(item => { %>
              <%
                let qtyClass = 'high';
                if (item.quantity <= (item.min_quantity || 5)) qtyClass = 'low';
                else if (item.quantity <= (item.min_quantity || 5) * 2) qtyClass = 'medium';
              %>
              <div class="stock-item" data-name="<%= item.name.toLowerCase() %>">
                <span class="item-name"><%= item.name %></span>
                <span class="item-qty <%= qtyClass %>"><%= item.quantity %> <%= item.unit || 'ชิ้น' %></span>
              </div>
            <% }) %>
          </div>
        <% } %>
      <% }) %>
    </div>

    <% if (items.length === 0) { %>
      <div class="text-center text-muted py-4">
        <i class="bi bi-inbox" style="font-size: 3rem;"></i>
        <p class="mt-2">ไม่มีข้อมูลวัสดุ</p>
      </div>
    <% } %>
  </div>

  <script>
    function filterStock() {
      const search = document.getElementById('searchInput').value.toLowerCase();
      document.querySelectorAll('.stock-item').forEach(item => {
        const name = item.dataset.name;
        item.style.display = name.includes(search) ? 'flex' : 'none';
      });
    }

    function filterCategory(btn, catId) {
      document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');

      document.querySelectorAll('.category-section').forEach(section => {
        section.style.display = (!catId || section.dataset.category === catId) ? 'block' : 'none';
      });
    }
  </script>
</body>
</html>
