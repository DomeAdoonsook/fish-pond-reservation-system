<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>จัดการ Stock วัสดุ - ระบบงานฟาร์ม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%);
      min-height: 100vh;
    }
    .hero-section {
      padding: 50px 0 30px;
      text-align: center;
    }
    .hero-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #2e7d32;
      margin-bottom: 10px;
    }
    .hero-subtitle {
      font-size: 1rem;
      color: #666;
      margin-bottom: 30px;
    }
    .menu-card {
      background: white;
      border-radius: 20px;
      padding: 35px 25px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
      height: 100%;
      border: none;
    }
    .menu-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 60px rgba(0,0,0,0.15);
    }
    .menu-icon {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      font-size: 2.5rem;
      color: white;
    }
    .menu-icon.items { background: linear-gradient(135deg, #4caf50, #388e3c); }
    .menu-icon.requests { background: linear-gradient(135deg, #ff9800, #f57c00); }
    .menu-icon.transactions { background: linear-gradient(135deg, #2196f3, #1976d2); }

    .menu-title {
      font-size: 1.4rem;
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 12px;
    }
    .menu-desc {
      color: #7f8c8d;
      font-size: 0.9rem;
      margin-bottom: 20px;
      line-height: 1.5;
    }
    .menu-btn {
      padding: 10px 30px;
      border-radius: 50px;
      font-weight: 500;
      font-size: 0.95rem;
      transition: all 0.3s;
    }
    .menu-btn.items { background: linear-gradient(135deg, #4caf50, #388e3c); border: none; }
    .menu-btn.items:hover { background: linear-gradient(135deg, #388e3c, #2e7d32); transform: scale(1.05); }
    .menu-btn.requests { background: linear-gradient(135deg, #ff9800, #f57c00); border: none; }
    .menu-btn.requests:hover { background: linear-gradient(135deg, #f57c00, #ef6c00); transform: scale(1.05); }
    .menu-btn.transactions { background: linear-gradient(135deg, #2196f3, #1976d2); border: none; }
    .menu-btn.transactions:hover { background: linear-gradient(135deg, #1976d2, #1565c0); transform: scale(1.05); }

    .badge-pending {
      position: absolute;
      top: 15px;
      right: 15px;
      font-size: 0.85rem;
      padding: 8px 12px;
      border-radius: 20px;
    }

    .stats-row {
      margin-top: 40px;
    }
    .stat-card {
      background: white;
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    }
    .stat-number {
      font-size: 1.8rem;
      font-weight: 700;
    }
    .stat-label {
      color: #7f8c8d;
      font-size: 0.85rem;
    }

    .footer-section {
      text-align: center;
      padding: 25px;
      color: #7f8c8d;
      font-size: 0.85rem;
    }
    .back-btn {
      position: fixed;
      top: 20px;
      left: 20px;
      background: white;
      color: #2e7d32;
      padding: 10px 20px;
      border-radius: 50px;
      font-size: 0.9rem;
      box-shadow: 0 5px 20px rgba(0,0,0,0.15);
      text-decoration: none;
      z-index: 1000;
    }
    .back-btn:hover {
      background: #2e7d32;
      color: white;
    }
    .alert-section {
      max-width: 800px;
      margin: 0 auto 30px;
    }
  </style>
</head>
<body>
  <!-- Back Button -->
  <a href="/admin" class="back-btn">
    <i class="bi bi-arrow-left me-2"></i>กลับหน้าหลัก
  </a>

  <div class="container">
    <!-- Hero Section -->
    <div class="hero-section">
      <h1 class="hero-title"><i class="bi bi-archive me-2"></i>Stock วัสดุเกษตร</h1>
      <p class="hero-subtitle">จัดการคลังวัสดุ เบิกจ่าย และติดตามสต็อก</p>
    </div>

    <!-- Low Stock Alert -->
    <% if (lowStockItems && lowStockItems.length > 0) { %>
      <div class="alert-section">
        <div class="alert alert-danger d-flex align-items-center">
          <i class="bi bi-exclamation-triangle-fill me-3 fs-4"></i>
          <div>
            <strong>วัสดุใกล้หมด!</strong>
            <% lowStockItems.forEach(function(item, index) { %>
              <%= item.name %> (เหลือ <%= item.current_quantity %> <%= item.unit %>)<%= index < lowStockItems.length - 1 ? ', ' : '' %>
            <% }); %>
          </div>
        </div>
      </div>
    <% } %>

    <!-- Menu Cards -->
    <div class="row g-4 justify-content-center">
      <!-- รายการวัสดุ -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <div class="menu-icon items">
            <i class="bi bi-box-seam"></i>
          </div>
          <h3 class="menu-title">รายการวัสดุ</h3>
          <p class="menu-desc">
            ดูรายการวัสดุทั้งหมด<br>
            เพิ่ม แก้ไข ลบ วัสดุ
          </p>
          <a href="/admin/stock/items" class="btn btn-success menu-btn items text-white">
            <i class="bi bi-list-ul me-2"></i>จัดการวัสดุ
          </a>
        </div>
      </div>

      <!-- คำขอเบิก -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (typeof stockPendingCount !== 'undefined' && stockPendingCount > 0) { %>
            <span class="badge bg-warning text-dark badge-pending"><%= stockPendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon requests">
            <i class="bi bi-clipboard-check"></i>
          </div>
          <h3 class="menu-title">คำขอเบิกวัสดุ</h3>
          <p class="menu-desc">
            อนุมัติ/ปฏิเสธคำขอ<br>
            ดูประวัติการเบิก
          </p>
          <a href="/admin/stock/requests" class="btn btn-warning menu-btn requests text-white">
            <i class="bi bi-check2-square me-2"></i>คำขอเบิก
          </a>
        </div>
      </div>

      <!-- รับ-จ่าย Stock -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <div class="menu-icon transactions">
            <i class="bi bi-arrow-left-right"></i>
          </div>
          <h3 class="menu-title">รับ-จ่าย Stock</h3>
          <p class="menu-desc">
            รับวัสดุเข้าคลัง<br>
            จ่ายออก ปรับยอด
          </p>
          <a href="/admin/stock/transactions" class="btn btn-primary menu-btn transactions text-white">
            <i class="bi bi-receipt me-2"></i>รับ-จ่าย Stock
          </a>
        </div>
      </div>

      <!-- รายงานการใช้น้ำมัน -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <div class="menu-icon" style="background: linear-gradient(135deg, #9c27b0, #7b1fa2);">
            <i class="bi bi-file-earmark-text"></i>
          </div>
          <h3 class="menu-title">รายงานการใช้น้ำมัน</h3>
          <p class="menu-desc">
            ดูรายงานการใช้น้ำมัน<br>
            แยกตามอุปกรณ์และเดือน
          </p>
          <a href="/admin/stock/fuel-report" class="btn menu-btn text-white" style="background: linear-gradient(135deg, #9c27b0, #7b1fa2); border: none;">
            <i class="bi bi-graph-up me-2"></i>ดูรายงาน
          </a>
        </div>
      </div>
    </div>

    <!-- Stats Row -->
    <div class="row g-3 stats-row justify-content-center">
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-success"><%= items.length %></div>
          <div class="stat-label">รายการทั้งหมด</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-primary"><%= items.filter(i => i.status === 'active' && i.current_quantity > i.min_quantity).length %></div>
          <div class="stat-label">พร้อมใช้งาน</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-danger"><%= lowStockItems.length %></div>
          <div class="stat-label">ใกล้หมด</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-warning"><%= categories.length %></div>
          <div class="stat-label">หมวดหมู่</div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer-section">
      ระบบงานฟาร์ม - คณะเทคโนโลยีการประมงและทรัพยากรทางน้ำ มหาวิทยาลัยแม่โจ้
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
