<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>กำลังใช้งาน - ระบบจัดการบ่อเลี้ยงปลา</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>
<body>
  <div class="wrapper">
    <%- include('../partials/sidebar-admin', { page: 'active', isLoggedIn: true, admin, pendingCount, cancelPendingCount: cancelPendingCount || 0, equipmentPendingCount: equipmentPendingCount || 0 }) %>

    <div id="content">
      <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container-fluid">
          <button type="button" id="sidebarCollapse" class="btn btn-dark"><i class="bi bi-list"></i></button>
          <span class="navbar-brand ms-3 mb-0 h5"><i class="bi bi-play-circle"></i> กำลังใช้งาน</span>
        </div>
      </nav>

      <div class="container-fluid p-4">
        <% if (reservations.length === 0) { %>
          <div class="card">
            <div class="card-body text-center py-5">
              <i class="bi bi-droplet text-muted" style="font-size: 4rem;"></i>
              <h5 class="mt-3 text-muted">ไม่มีบ่อที่กำลังใช้งาน</h5>
            </div>
          </div>
        <% } else { %>
          <div class="table-responsive">
            <table class="table table-hover bg-white rounded shadow-sm">
              <thead class="table-dark">
                <tr>
                  <th>บ่อ</th>
                  <th>ผู้ใช้งาน</th>
                  <th>ชนิดปลา</th>
                  <th>จำนวน</th>
                  <th>อายุปลา</th>
                  <th>วันสิ้นสุด</th>
                  <th>เหลือ</th>
                  <th>การดำเนินการ</th>
                </tr>
              </thead>
              <tbody>
                <% reservations.forEach(function(r) { %>
                  <tr id="active-<%= r.id %>">
                    <td><a href="/admin/pond/<%= r.pond_id %>" class="fw-bold text-decoration-none"><%= r.pond_code %></a></td>
                    <td><%= r.user_name %></td>
                    <td><%= r.fish_type %></td>
                    <td><%= r.fish_quantity ? r.fish_quantity.toLocaleString() : 0 %> ตัว</td>
                    <td><span class="badge bg-info"><%= r.fish_age_days || 0 %> วัน</span></td>
                    <td><%= r.end_date %></td>
                    <td>
                      <% if (r.days_remaining <= 7) { %>
                        <span class="badge bg-danger"><%= r.days_remaining %> วัน</span>
                      <% } else if (r.days_remaining <= 30) { %>
                        <span class="badge bg-warning text-dark"><%= r.days_remaining %> วัน</span>
                      <% } else { %>
                        <span class="badge bg-success"><%= r.days_remaining %> วัน</span>
                      <% } %>
                    </td>
                    <td>
                      <button class="btn btn-sm btn-outline-primary" onclick="completeReservation(<%= r.id %>)">
                        <i class="bi bi-check2-circle"></i> คืนบ่อ
                      </button>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
  <script>
    async function completeReservation(id) {
      if (!confirm('ยืนยันการคืนบ่อ? การดำเนินการนี้จะสิ้นสุดการใช้บ่อ')) return;
      try {
        const res = await fetch('/admin/reservations/' + id + '/complete', { method: 'POST', headers: { 'Content-Type': 'application/json' } });
        const data = await res.json();
        if (data.success) { document.getElementById('active-' + id).remove(); showToast('คืนบ่อเรียบร้อย', 'success'); }
      } catch (err) { showToast('เกิดข้อผิดพลาด', 'danger'); }
    }
  </script>
</body>
</html>
