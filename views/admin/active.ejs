<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>กำลังใช้งาน - ระบบงานฟาร์ม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #e4e8ec 100%);
      min-height: 100vh;
    }
    .page-header {
      background: linear-gradient(135deg, #4caf50, #388e3c);
      color: white;
      padding: 25px 30px;
      border-radius: 0 0 20px 20px;
      margin-bottom: 25px;
    }
    .back-btn {
      background: rgba(255,255,255,0.2);
      color: white;
      padding: 8px 16px;
      border-radius: 50px;
      text-decoration: none;
      font-size: 0.9rem;
    }
    .back-btn:hover {
      background: rgba(255,255,255,0.3);
      color: white;
    }
  </style>
</head>
<body>
  <div class="container-fluid px-0">
    <!-- Header -->
    <div class="page-header">
      <div class="container">
        <div class="d-flex justify-content-between align-items-center">
          <div>
            <a href="/admin/ponds" class="back-btn mb-2 d-inline-block">
              <i class="bi bi-arrow-left me-1"></i>กลับ
            </a>
            <h2 class="mb-1"><i class="bi bi-play-circle me-2"></i>กำลังใช้งาน</h2>
            <p class="mb-0 opacity-75">รายการบ่อที่กำลังใช้งานอยู่</p>
          </div>
          <div>
            <span class="badge bg-light text-dark fs-6"><%= reservations.length %> รายการ</span>
          </div>
        </div>
      </div>
    </div>

    <div class="container pb-4">
      <% if (reservations.length === 0) { %>
        <div class="card">
          <div class="card-body text-center py-5">
            <i class="bi bi-droplet text-muted" style="font-size: 4rem;"></i>
            <h5 class="mt-3 text-muted">ไม่มีบ่อที่กำลังใช้งาน</h5>
            <a href="/admin/ponds" class="btn btn-primary mt-3"><i class="bi bi-arrow-left me-2"></i>กลับหน้าบ่อปลา</a>
          </div>
        </div>
      <% } else { %>
        <div class="table-responsive">
          <table class="table table-hover bg-white rounded shadow-sm">
            <thead class="table-dark">
              <tr>
                <th>บ่อ</th>
                <th>ผู้ใช้งาน</th>
                <th>ชนิดปลา</th>
                <th>จำนวน</th>
                <th>อายุปลา</th>
                <th>วันสิ้นสุด</th>
                <th>เหลือ</th>
                <th>การดำเนินการ</th>
              </tr>
            </thead>
            <tbody>
              <% reservations.forEach(function(r) { %>
                <tr id="active-<%= r.id %>">
                  <td><a href="/admin/pond/<%= r.pond_id %>" class="fw-bold text-decoration-none"><%= r.pond_code %></a></td>
                  <td><%= r.user_name %></td>
                  <td><%= r.fish_type %></td>
                  <td><%= r.fish_quantity ? r.fish_quantity.toLocaleString() : 0 %> ตัว</td>
                  <td><span class="badge bg-info"><%= r.fish_age_days || 0 %> วัน</span></td>
                  <td><%= r.end_date %></td>
                  <td>
                    <% if (r.days_remaining <= 7) { %>
                      <span class="badge bg-danger"><%= r.days_remaining %> วัน</span>
                    <% } else if (r.days_remaining <= 30) { %>
                      <span class="badge bg-warning text-dark"><%= r.days_remaining %> วัน</span>
                    <% } else { %>
                      <span class="badge bg-success"><%= r.days_remaining %> วัน</span>
                    <% } %>
                  </td>
                  <td>
                    <button class="btn btn-sm btn-outline-primary" onclick="completeReservation(<%= r.id %>)">
                      <i class="bi bi-check2-circle"></i> คืนบ่อ
                    </button>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function showToast(message, type) {
      alert(message);
    }

    async function completeReservation(id) {
      if (!confirm('ยืนยันการคืนบ่อ? การดำเนินการนี้จะสิ้นสุดการใช้บ่อ')) return;
      try {
        const res = await fetch('/admin/reservations/' + id + '/complete', { method: 'POST', headers: { 'Content-Type': 'application/json' } });
        const data = await res.json();
        if (data.success) {
          document.getElementById('active-' + id).remove();
          showToast('คืนบ่อเรียบร้อย', 'success');
          if (document.querySelectorAll('#active tbody tr').length === 0) {
            location.reload();
          }
        }
      } catch (err) { showToast('เกิดข้อผิดพลาด', 'danger'); }
    }
  </script>
</body>
</html>
