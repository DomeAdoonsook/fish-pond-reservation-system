<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ยืม-คืน อุปกรณ์ - ระบบงานฟาร์ม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #fff3e0 0%, #ffe0b2 100%);
      min-height: 100vh;
    }
    .hero-section {
      padding: 50px 0 30px;
      text-align: center;
    }
    .hero-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #e65100;
      margin-bottom: 10px;
    }
    .hero-subtitle {
      font-size: 1rem;
      color: #666;
      margin-bottom: 30px;
    }
    .menu-card {
      background: white;
      border-radius: 20px;
      padding: 35px 25px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
      height: 100%;
      border: none;
    }
    .menu-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 60px rgba(0,0,0,0.15);
    }
    .menu-icon {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      font-size: 2.5rem;
      color: white;
    }
    .menu-icon.items { background: linear-gradient(135deg, #ff9800, #f57c00); }
    .menu-icon.requests { background: linear-gradient(135deg, #e91e63, #c2185b); }
    .menu-icon.borrowed { background: linear-gradient(135deg, #9c27b0, #7b1fa2); }
    .menu-icon.history { background: linear-gradient(135deg, #607d8b, #455a64); }

    .menu-title {
      font-size: 1.4rem;
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 12px;
    }
    .menu-desc {
      color: #7f8c8d;
      font-size: 0.9rem;
      margin-bottom: 20px;
      line-height: 1.5;
    }
    .menu-btn {
      padding: 10px 30px;
      border-radius: 50px;
      font-weight: 500;
      font-size: 0.95rem;
      transition: all 0.3s;
    }
    .menu-btn.items { background: linear-gradient(135deg, #ff9800, #f57c00); border: none; }
    .menu-btn.items:hover { background: linear-gradient(135deg, #f57c00, #ef6c00); transform: scale(1.05); }
    .menu-btn.requests { background: linear-gradient(135deg, #e91e63, #c2185b); border: none; }
    .menu-btn.requests:hover { background: linear-gradient(135deg, #c2185b, #ad1457); transform: scale(1.05); }
    .menu-btn.borrowed { background: linear-gradient(135deg, #9c27b0, #7b1fa2); border: none; }
    .menu-btn.borrowed:hover { background: linear-gradient(135deg, #7b1fa2, #6a1b9a); transform: scale(1.05); }
    .menu-btn.history { background: linear-gradient(135deg, #607d8b, #455a64); border: none; }
    .menu-btn.history:hover { background: linear-gradient(135deg, #455a64, #37474f); transform: scale(1.05); }

    .badge-pending {
      position: absolute;
      top: 15px;
      right: 15px;
      font-size: 0.85rem;
      padding: 8px 12px;
      border-radius: 20px;
    }

    .stats-row {
      margin-top: 40px;
    }
    .stat-card {
      background: white;
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    }
    .stat-number {
      font-size: 1.8rem;
      font-weight: 700;
    }
    .stat-label {
      color: #7f8c8d;
      font-size: 0.85rem;
    }

    .footer-section {
      text-align: center;
      padding: 25px;
      color: #7f8c8d;
      font-size: 0.85rem;
    }
    .back-btn {
      position: fixed;
      top: 20px;
      left: 20px;
      background: white;
      color: #e65100;
      padding: 10px 20px;
      border-radius: 50px;
      font-size: 0.9rem;
      box-shadow: 0 5px 20px rgba(0,0,0,0.15);
      text-decoration: none;
      z-index: 1000;
    }
    .back-btn:hover {
      background: #e65100;
      color: white;
    }
  </style>
</head>
<body>
  <!-- Back Button -->
  <a href="/admin" class="back-btn">
    <i class="bi bi-arrow-left me-2"></i>กลับหน้าหลัก
  </a>

  <div class="container">
    <!-- Hero Section -->
    <div class="hero-section">
      <h1 class="hero-title"><i class="bi bi-tools me-2"></i>ยืม-คืน อุปกรณ์</h1>
      <p class="hero-subtitle">จัดการอุปกรณ์ อนุมัติคำขอยืม ติดตามการคืน</p>
    </div>

    <!-- Menu Cards -->
    <div class="row g-4 justify-content-center">
      <!-- รายการอุปกรณ์ -->
      <div class="col-md-6 col-lg-3">
        <div class="menu-card position-relative">
          <div class="menu-icon items">
            <i class="bi bi-box-seam"></i>
          </div>
          <h3 class="menu-title">รายการอุปกรณ์</h3>
          <p class="menu-desc">
            ดูรายการอุปกรณ์<br>
            เพิ่ม แก้ไข ลบ
          </p>
          <a href="/admin/equipment/list" class="btn menu-btn items text-white">
            <i class="bi bi-list-ul me-2"></i>จัดการ
          </a>
        </div>
      </div>

      <!-- คำขอยืม -->
      <div class="col-md-6 col-lg-3">
        <div class="menu-card position-relative">
          <% if (typeof equipmentPendingCount !== 'undefined' && equipmentPendingCount > 0) { %>
            <span class="badge bg-danger badge-pending"><%= equipmentPendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon requests">
            <i class="bi bi-clipboard-check"></i>
          </div>
          <h3 class="menu-title">คำขอยืม</h3>
          <p class="menu-desc">
            อนุมัติ/ปฏิเสธ<br>
            คำขอยืมอุปกรณ์
          </p>
          <a href="/admin/equipment/requests" class="btn menu-btn requests text-white">
            <i class="bi bi-check2-square me-2"></i>คำขอยืม
          </a>
        </div>
      </div>

      <!-- กำลังยืม -->
      <div class="col-md-6 col-lg-3">
        <div class="menu-card position-relative">
          <div class="menu-icon borrowed">
            <i class="bi bi-person-badge"></i>
          </div>
          <h3 class="menu-title">กำลังยืม</h3>
          <p class="menu-desc">
            ติดตามอุปกรณ์<br>
            ที่ถูกยืมอยู่
          </p>
          <a href="/admin/equipment/borrowed" class="btn menu-btn borrowed text-white">
            <i class="bi bi-hourglass-split me-2"></i>กำลังยืม
          </a>
        </div>
      </div>

      <!-- ประวัติ -->
      <div class="col-md-6 col-lg-3">
        <div class="menu-card position-relative">
          <div class="menu-icon history">
            <i class="bi bi-clock-history"></i>
          </div>
          <h3 class="menu-title">ประวัติ</h3>
          <p class="menu-desc">
            ดูประวัติ<br>
            การยืม-คืน
          </p>
          <a href="/admin/equipment/history" class="btn menu-btn history text-white">
            <i class="bi bi-journal-text me-2"></i>ประวัติ
          </a>
        </div>
      </div>
    </div>

    <!-- Stats Row -->
    <div class="row g-3 stats-row justify-content-center">
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-warning"><%= equipment.length %></div>
          <div class="stat-label">รายการอุปกรณ์</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-success"><%= equipment.reduce((sum, e) => sum + (e.available_quantity || 0), 0) %></div>
          <div class="stat-label">พร้อมยืม</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-danger"><%= equipment.reduce((sum, e) => sum + (e.borrowed_quantity || 0), 0) %></div>
          <div class="stat-label">ถูกยืมอยู่</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-primary"><%= categories.length %></div>
          <div class="stat-label">หมวดหมู่</div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer-section">
      ระบบงานฟาร์ม - คณะเทคโนโลยีการประมงและทรัพยากรทางน้ำ มหาวิทยาลัยแม่โจ้
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
