<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>จัดการบ่อปลา - ระบบงานฟาร์ม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
      min-height: 100vh;
    }
    .hero-section {
      padding: 50px 0 30px;
      text-align: center;
    }
    .hero-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #1565c0;
      margin-bottom: 10px;
    }
    .hero-subtitle {
      font-size: 1rem;
      color: #666;
      margin-bottom: 30px;
    }
    .menu-card {
      background: white;
      border-radius: 20px;
      padding: 35px 25px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
      height: 100%;
      border: none;
    }
    .menu-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 60px rgba(0,0,0,0.15);
    }
    .menu-icon {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      font-size: 2.5rem;
      color: white;
    }
    .menu-icon.map { background: linear-gradient(135deg, #2196f3, #1976d2); }
    .menu-icon.requests { background: linear-gradient(135deg, #ff9800, #f57c00); }
    .menu-icon.active { background: linear-gradient(135deg, #4caf50, #388e3c); }
    .menu-icon.cancel { background: linear-gradient(135deg, #f44336, #d32f2f); }
    .menu-icon.history { background: linear-gradient(135deg, #9c27b0, #7b1fa2); }

    .menu-title {
      font-size: 1.4rem;
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 12px;
    }
    .menu-desc {
      color: #7f8c8d;
      font-size: 0.9rem;
      margin-bottom: 20px;
      line-height: 1.5;
    }
    .menu-btn {
      padding: 10px 30px;
      border-radius: 50px;
      font-weight: 500;
      font-size: 0.95rem;
      transition: all 0.3s;
    }
    .menu-btn.map { background: linear-gradient(135deg, #2196f3, #1976d2); border: none; }
    .menu-btn.map:hover { background: linear-gradient(135deg, #1976d2, #1565c0); transform: scale(1.05); }
    .menu-btn.requests { background: linear-gradient(135deg, #ff9800, #f57c00); border: none; }
    .menu-btn.requests:hover { background: linear-gradient(135deg, #f57c00, #ef6c00); transform: scale(1.05); }
    .menu-btn.active { background: linear-gradient(135deg, #4caf50, #388e3c); border: none; }
    .menu-btn.active:hover { background: linear-gradient(135deg, #388e3c, #2e7d32); transform: scale(1.05); }
    .menu-btn.cancel { background: linear-gradient(135deg, #f44336, #d32f2f); border: none; }
    .menu-btn.cancel:hover { background: linear-gradient(135deg, #d32f2f, #c62828); transform: scale(1.05); }
    .menu-btn.history { background: linear-gradient(135deg, #9c27b0, #7b1fa2); border: none; }
    .menu-btn.history:hover { background: linear-gradient(135deg, #7b1fa2, #6a1b9a); transform: scale(1.05); }

    .badge-pending {
      position: absolute;
      top: 15px;
      right: 15px;
      font-size: 0.85rem;
      padding: 8px 12px;
      border-radius: 20px;
    }

    .stats-row {
      margin-top: 40px;
    }
    .stat-card {
      background: white;
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    }
    .stat-number {
      font-size: 1.8rem;
      font-weight: 700;
    }
    .stat-label {
      color: #7f8c8d;
      font-size: 0.85rem;
    }

    .footer-section {
      text-align: center;
      padding: 25px;
      color: #7f8c8d;
      font-size: 0.85rem;
    }
    .back-btn {
      position: fixed;
      top: 20px;
      left: 20px;
      background: white;
      color: #1565c0;
      padding: 10px 20px;
      border-radius: 50px;
      font-size: 0.9rem;
      box-shadow: 0 5px 20px rgba(0,0,0,0.15);
      text-decoration: none;
      z-index: 1000;
    }
    .back-btn:hover {
      background: #1565c0;
      color: white;
    }
  </style>
</head>
<body>
  <!-- Back Button -->
  <a href="/admin" class="back-btn">
    <i class="bi bi-arrow-left me-2"></i>กลับหน้าหลัก
  </a>

  <div class="container">
    <!-- Hero Section -->
    <div class="hero-section">
      <h1 class="hero-title"><i class="bi bi-droplet-half me-2"></i>บ่อเลี้ยงปลา</h1>
      <p class="hero-subtitle">จัดการบ่อปลา อนุมัติการจอง ดูประวัติ</p>
    </div>

    <!-- Menu Cards -->
    <div class="row g-4 justify-content-center">
      <!-- ผังบ่อปลา -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <div class="menu-icon map">
            <i class="bi bi-grid-3x3-gap"></i>
          </div>
          <h3 class="menu-title">ผังบ่อปลา</h3>
          <p class="menu-desc">
            ดูผังบ่อทั้งหมด<br>
            สถานะบ่อแบบ Real-time
          </p>
          <a href="/admin/ponds/map" class="btn btn-primary menu-btn map text-white">
            <i class="bi bi-map me-2"></i>ดูผังบ่อ
          </a>
        </div>
      </div>

      <!-- คำขอจองบ่อ -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (pendingCount > 0) { %>
            <span class="badge bg-warning text-dark badge-pending"><%= pendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon requests">
            <i class="bi bi-clipboard-check"></i>
          </div>
          <h3 class="menu-title">คำขอจองบ่อ</h3>
          <p class="menu-desc">
            อนุมัติ/ปฏิเสธคำขอ<br>
            ตรวจสอบรายละเอียด
          </p>
          <a href="/admin/requests" class="btn btn-warning menu-btn requests text-white">
            <i class="bi bi-check2-square me-2"></i>คำขอจอง
          </a>
        </div>
      </div>

      <!-- กำลังใช้งาน -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (activeCount > 0) { %>
            <span class="badge bg-success badge-pending"><%= activeCount %> กำลังใช้</span>
          <% } %>
          <div class="menu-icon active">
            <i class="bi bi-play-circle"></i>
          </div>
          <h3 class="menu-title">กำลังใช้งาน</h3>
          <p class="menu-desc">
            ดูรายการที่กำลังใช้บ่อ<br>
            คืนบ่อ ยกเลิกการจอง
          </p>
          <a href="/admin/active" class="btn btn-success menu-btn active text-white">
            <i class="bi bi-activity me-2"></i>กำลังใช้งาน
          </a>
        </div>
      </div>

      <!-- คำขอยกเลิก -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (cancelPendingCount > 0) { %>
            <span class="badge bg-danger badge-pending"><%= cancelPendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon cancel">
            <i class="bi bi-x-circle"></i>
          </div>
          <h3 class="menu-title">คำขอยกเลิก</h3>
          <p class="menu-desc">
            อนุมัติ/ปฏิเสธคำขอยกเลิก<br>
            จากผู้ใช้งาน
          </p>
          <a href="/admin/cancel-requests" class="btn btn-danger menu-btn cancel text-white">
            <i class="bi bi-x-lg me-2"></i>คำขอยกเลิก
          </a>
        </div>
      </div>

      <!-- ประวัติการจอง -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <div class="menu-icon history">
            <i class="bi bi-clock-history"></i>
          </div>
          <h3 class="menu-title">ประวัติการจอง</h3>
          <p class="menu-desc">
            ดูประวัติการจองทั้งหมด<br>
            ค้นหา กรองข้อมูล
          </p>
          <a href="/admin/history" class="btn menu-btn history text-white">
            <i class="bi bi-journal-text me-2"></i>ประวัติ
          </a>
        </div>
      </div>
    </div>

    <!-- Stats Row -->
    <div class="row g-3 stats-row justify-content-center">
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-primary"><%= status.available %></div>
          <div class="stat-label">บ่อว่าง</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-danger"><%= status.occupied %></div>
          <div class="stat-label">ใช้งานอยู่</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-warning"><%= status.pending %></div>
          <div class="stat-label">รออนุมัติ</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-secondary"><%= status.maintenance %></div>
          <div class="stat-label">ปิดปรับปรุง</div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer-section">
      ระบบงานฟาร์ม - คณะเทคโนโลยีการประมงและทรัพยากรทางน้ำ มหาวิทยาลัยแม่โจ้
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
