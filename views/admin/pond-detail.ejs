<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>รายละเอียดบ่อ <%= pond.pond_code %> - ระบบงานฟาร์ม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>
<body>
  <div class="wrapper">
    <%- include('../partials/sidebar-admin', { page: 'pond-detail', isLoggedIn: typeof isLoggedIn !== 'undefined' ? isLoggedIn : true, admin, pendingCount, cancelPendingCount: cancelPendingCount || 0, equipmentPendingCount: equipmentPendingCount || 0 }) %>

    <!-- Page Content -->
    <div id="content">
      <!-- Top Navbar -->
      <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container-fluid">
          <button type="button" id="sidebarCollapse" class="btn btn-dark">
            <i class="bi bi-list"></i>
          </button>
          <span class="navbar-brand ms-3 mb-0 h5">
            <i class="bi bi-droplet"></i> รายละเอียดบ่อ <%= pond.pond_code %>
          </span>
          <div class="ms-auto">
            <a href="/admin" class="btn btn-outline-secondary btn-sm">
              <i class="bi bi-arrow-left"></i> กลับ
            </a>
          </div>
        </div>
      </nav>

      <!-- Main Content -->
      <div class="container-fluid p-4">
        <div class="row">
          <!-- Pond Info -->
          <div class="col-lg-4 mb-4">
            <div class="card h-100">
              <div class="card-header">
                <h5 class="mb-0">
                  <i class="bi bi-droplet me-2"></i>บ่อ <%= pond.pond_code %>
                </h5>
              </div>
              <div class="card-body">
                <table class="table table-borderless">
                  <tr>
                    <td class="text-muted">โซน:</td>
                    <td class="fw-bold"><%= pond.zone %></td>
                  </tr>
                  <tr>
                    <td class="text-muted">ขนาด:</td>
                    <td>
                      <%= pond.size === 'large' ? 'ใหญ่' : pond.size === 'medium' ? 'กลาง' : 'เล็ก' %>
                      <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                        <button class="btn btn-sm btn-outline-primary ms-2" data-bs-toggle="modal" data-bs-target="#editSizeModal">
                          <i class="bi bi-pencil"></i>
                        </button>
                      <% } %>
                    </td>
                  </tr>
                  <tr>
                    <td class="text-muted">สถานะ:</td>
                    <td>
                      <% if (pond.status === 'maintenance') { %>
                        <span class="badge bg-secondary">ปิดปรับปรุง</span>
                      <% } else if (pond.user_name && pond.is_active) { %>
                        <span class="badge bg-danger">ใช้งาน</span>
                      <% } else if (pond.user_name && !pond.is_active) { %>
                        <span class="badge bg-info">ได้รับอนุมัติ รอเริ่มใช้งาน</span>
                      <% } else { %>
                        <span class="badge bg-success">ว่าง</span>
                      <% } %>
                    </td>
                  </tr>
                </table>

                <% if (!pond.user_name && pond.status !== 'maintenance') { %>
                  <hr>
                  <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                    <button class="btn btn-primary w-100" data-bs-toggle="modal" data-bs-target="#setupPondModal">
                      <i class="bi bi-gear me-2"></i>ตั้งค่าการใช้งานบ่อนี้
                    </button>
                  <% } else { %>
                    <a href="/booking/<%= pond.id %>" class="btn btn-primary w-100">
                      <i class="bi bi-calendar-plus me-2"></i>ขอใช้บ่อนี้
                    </a>
                  <% } %>
                <% } %>

                <% if (pond.user_name) { %>
                  <hr>
                  <h6><i class="bi bi-person me-2"></i>ข้อมูลการใช้งานปัจจุบัน</h6>
                  <table class="table table-sm table-borderless">
                    <tr>
                      <td class="text-muted">ผู้ใช้:</td>
                      <td class="fw-bold"><%= pond.user_name %></td>
                    </tr>
                    <tr>
                      <td class="text-muted">ชนิดปลา:</td>
                      <td><%= pond.fish_type %></td>
                    </tr>
                    <tr>
                      <td class="text-muted">จำนวน:</td>
                      <td><%= pond.fish_quantity ? pond.fish_quantity.toLocaleString() : '-' %> ตัว</td>
                    </tr>
                    <tr>
                      <td class="text-muted">วันลงปลา:</td>
                      <td><%= pond.start_date || '-' %></td>
                    </tr>
                    <tr>
                      <td class="text-muted">อายุปลา:</td>
                      <td><span class="badge bg-info"><%= pond.fish_age_days || 0 %> วัน</span></td>
                    </tr>
                  </table>
                <% } %>

                <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                  <hr>
                  <h6><i class="bi bi-gear me-2"></i>จัดการบ่อ</h6>
                  <div class="d-grid gap-2">
                    <% if (pond.user_name && pond.reservation_id) { %>
                      <button class="btn btn-warning" onclick="completeReservation(<%= pond.reservation_id %>)">
                        <i class="bi bi-check2-circle me-2"></i>คืนบ่อ
                      </button>
                      <button class="btn btn-danger" onclick="showCancelModal(<%= pond.reservation_id %>)">
                        <i class="bi bi-x-circle me-2"></i>ยกเลิกการใช้บ่อ
                      </button>
                    <% } %>
                    <% if (pond.status !== 'maintenance') { %>
                      <button class="btn btn-secondary" onclick="setMaintenance(<%= pond.id %>)">
                        <i class="bi bi-tools me-2"></i>ปิดปรับปรุง
                      </button>
                    <% } else { %>
                      <button class="btn btn-success" onclick="setAvailable(<%= pond.id %>)">
                        <i class="bi bi-check-circle me-2"></i>เปิดใช้งาน
                      </button>
                    <% } %>
                  </div>
                <% } %>
              </div>
            </div>
          </div>

          <!-- History -->
          <div class="col-lg-8 mb-4">
            <div class="card h-100">
              <div class="card-header">
                <h5 class="mb-0">
                  <i class="bi bi-clock-history me-2"></i>ประวัติการใช้งาน
                </h5>
              </div>
              <div class="card-body p-0">
                <% if (history.length === 0) { %>
                  <div class="text-center py-5 text-muted">
                    <i class="bi bi-inbox" style="font-size: 3rem;"></i>
                    <p class="mt-2">ยังไม่มีประวัติการใช้งาน</p>
                  </div>
                <% } else { %>
                  <div class="table-responsive">
                    <table class="table table-hover mb-0">
                      <thead class="table-light">
                        <tr>
                          <th>รหัส</th>
                          <th>ผู้ขอ</th>
                          <th>ปลา</th>
                          <th>ระยะเวลา</th>
                          <th>สถานะ</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% history.forEach(function(h) { %>
                          <tr>
                            <td>#REQ-<%= String(h.id).padStart(4, '0') %></td>
                            <td><%= h.user_name %></td>
                            <td><%= h.fish_type %> (<%= h.fish_quantity ? h.fish_quantity.toLocaleString() : '-' %>)</td>
                            <td><%= h.start_date %> - <%= h.end_date %></td>
                            <td>
                              <% if (h.status === 'pending') { %>
                                <span class="badge bg-warning text-dark">รออนุมัติ</span>
                              <% } else if (h.status === 'approved') { %>
                                <span class="badge bg-success">อนุมัติแล้ว</span>
                              <% } else if (h.status === 'rejected') { %>
                                <span class="badge bg-danger">ไม่อนุมัติ</span>
                              <% } else if (h.status === 'cancelled') { %>
                                <span class="badge bg-secondary">ยกเลิก</span>
                              <% } else if (h.status === 'completed') { %>
                                <span class="badge bg-info">เสร็จสิ้น</span>
                              <% } %>
                            </td>
                          </tr>
                        <% }); %>
                      </tbody>
                    </table>
                  </div>
                <% } %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Setup Pond Modal (สำหรับ Admin ตั้งค่าการใช้งานบ่อ) -->
  <div class="modal fade" id="setupPondModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header bg-primary text-white">
          <h5 class="modal-title"><i class="bi bi-gear me-2"></i>ตั้งค่าการใช้งานบ่อ <%= pond.pond_code %></h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <form id="setupPondForm">
            <div class="mb-3">
              <label class="form-label">ชื่อผู้ใช้บ่อ <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="setupUserName" required placeholder="ระบุชื่อผู้ใช้บ่อ">
            </div>
            <div class="mb-3">
              <label class="form-label">ชนิดปลา <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="setupFishType" required placeholder="เช่น ปลานิล, ปลาดุก">
            </div>
            <div class="mb-3">
              <label class="form-label">จำนวนปลา (ตัว) <span class="text-danger">*</span></label>
              <input type="number" class="form-control" id="setupFishQuantity" required min="1" placeholder="จำนวนปลา">
            </div>
            <div class="row">
              <div class="col-md-6 mb-3">
                <label class="form-label">วันเริ่มต้น <span class="text-danger">*</span></label>
                <input type="date" class="form-control" id="setupStartDate" required>
              </div>
              <div class="col-md-6 mb-3">
                <label class="form-label">วันสิ้นสุด <span class="text-danger">*</span></label>
                <input type="date" class="form-control" id="setupEndDate" required>
              </div>
            </div>
            <div class="mb-3">
              <label class="form-label">เบอร์โทร</label>
              <input type="tel" class="form-control" id="setupPhone" placeholder="เบอร์โทรติดต่อ (ถ้ามี)">
            </div>
            <div class="mb-3">
              <label class="form-label">วัตถุประสงค์</label>
              <textarea class="form-control" id="setupPurpose" rows="2" placeholder="วัตถุประสงค์ในการใช้บ่อ (ถ้ามี)"></textarea>
            </div>
          </form>
          <div class="alert alert-info">
            <i class="bi bi-info-circle me-2"></i>
            การจองนี้จะถูกอนุมัติทันทีโดย Admin และจะไม่ส่งแจ้งเตือน LINE
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
          <button type="button" class="btn btn-primary" id="confirmSetupBtn">
            <i class="bi bi-check-circle me-2"></i>ยืนยันการตั้งค่า
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Edit Size Modal -->
  <div class="modal fade" id="editSizeModal" tabindex="-1">
    <div class="modal-dialog modal-sm">
      <div class="modal-content">
        <div class="modal-header bg-primary text-white">
          <h5 class="modal-title"><i class="bi bi-pencil me-2"></i>แก้ไขขนาดบ่อ</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <label class="form-label">ขนาดบ่อ</label>
          <select class="form-select" id="editPondSize">
            <option value="small" <%= pond.size === 'small' ? 'selected' : '' %>>เล็ก</option>
            <option value="medium" <%= pond.size === 'medium' ? 'selected' : '' %>>กลาง</option>
            <option value="large" <%= pond.size === 'large' ? 'selected' : '' %>>ใหญ่</option>
          </select>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
          <button type="button" class="btn btn-primary" id="confirmEditSizeBtn">
            <i class="bi bi-check-circle me-2"></i>บันทึก
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Cancel Reservation Modal -->
  <div class="modal fade" id="cancelModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header bg-danger text-white">
          <h5 class="modal-title"><i class="bi bi-x-circle me-2"></i>ยกเลิกการใช้บ่อ</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <p>คุณต้องการยกเลิกการใช้บ่อนี้หรือไม่?</p>
          <div class="mb-3">
            <label class="form-label">เหตุผลในการยกเลิก (ไม่บังคับ)</label>
            <textarea class="form-control" id="cancelReason" rows="3" placeholder="ระบุเหตุผล..."></textarea>
          </div>
          <div class="alert alert-warning">
            <i class="bi bi-exclamation-triangle me-2"></i>
            การยกเลิกจะส่งแจ้งเตือนไปยังผู้ขอทาง LINE
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ปิด</button>
          <button type="button" class="btn btn-danger" id="confirmCancelBtn">
            <i class="bi bi-x-circle me-2"></i>ยืนยันยกเลิก
          </button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
  <script>
    async function completeReservation(id) {
      if (!confirm('ยืนยันการคืนบ่อ?')) return;
      try {
        const res = await fetch('/admin/reservations/' + id + '/complete', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' }
        });
        if (res.ok) {
          alert('คืนบ่อเรียบร้อย');
          location.reload();
        }
      } catch (err) {
        alert('เกิดข้อผิดพลาด');
      }
    }

    // Edit pond size
    document.getElementById('confirmEditSizeBtn')?.addEventListener('click', async function() {
      const size = document.getElementById('editPondSize').value;

      this.disabled = true;
      this.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>กำลังบันทึก...';

      try {
        const res = await fetch('/admin/pond/<%= pond.id %>/size', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ size })
        });

        if (res.ok) {
          alert('บันทึกขนาดบ่อเรียบร้อย');
          location.reload();
        } else {
          const data = await res.json();
          alert('เกิดข้อผิดพลาด: ' + (data.error || 'ไม่สามารถบันทึกได้'));
        }
      } catch (err) {
        alert('เกิดข้อผิดพลาด');
      } finally {
        this.disabled = false;
        this.innerHTML = '<i class="bi bi-check-circle me-2"></i>บันทึก';
      }
    });

    async function setMaintenance(id) {
      if (!confirm('ยืนยันการปิดปรับปรุงบ่อ?')) return;
      try {
        const res = await fetch('/admin/pond/' + id + '/status', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ status: 'maintenance' })
        });
        if (res.ok) {
          alert('ปิดปรับปรุงเรียบร้อย');
          location.reload();
        }
      } catch (err) {
        alert('เกิดข้อผิดพลาด');
      }
    }

    async function setAvailable(id) {
      if (!confirm('ยืนยันการเปิดใช้งานบ่อ?')) return;
      try {
        const res = await fetch('/admin/pond/' + id + '/status', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ status: 'available' })
        });
        if (res.ok) {
          alert('เปิดใช้งานเรียบร้อย');
          location.reload();
        }
      } catch (err) {
        alert('เกิดข้อผิดพลาด');
      }
    }

    // Cancel reservation
    let cancelReservationId = null;
    const cancelModal = new bootstrap.Modal(document.getElementById('cancelModal'));

    function showCancelModal(id) {
      cancelReservationId = id;
      document.getElementById('cancelReason').value = '';
      cancelModal.show();
    }

    document.getElementById('confirmCancelBtn').addEventListener('click', async function() {
      if (!cancelReservationId) return;

      const reason = document.getElementById('cancelReason').value;

      try {
        const res = await fetch('/admin/reservations/' + cancelReservationId + '/cancel', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ reason: reason })
        });

        if (res.ok) {
          cancelModal.hide();
          alert('ยกเลิกการใช้บ่อเรียบร้อย');
          location.reload();
        } else {
          const data = await res.json();
          alert('เกิดข้อผิดพลาด: ' + (data.error || 'ไม่สามารถยกเลิกได้'));
        }
      } catch (err) {
        alert('เกิดข้อผิดพลาด');
      }
    });

    // Setup Pond Modal (Admin สร้างการจองโดยตรง)
    const setupPondModal = document.getElementById('setupPondModal');
    if (setupPondModal) {
      // ตั้งค่าวันที่เริ่มต้นเป็นวันนี้
      setupPondModal.addEventListener('show.bs.modal', function() {
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('setupStartDate').value = today;
        // ตั้งค่าวันสิ้นสุดเป็น 3 เดือนข้างหน้า
        const threeMonthsLater = new Date();
        threeMonthsLater.setMonth(threeMonthsLater.getMonth() + 3);
        document.getElementById('setupEndDate').value = threeMonthsLater.toISOString().split('T')[0];
      });

      document.getElementById('confirmSetupBtn').addEventListener('click', async function() {
        const userName = document.getElementById('setupUserName').value.trim();
        const fishType = document.getElementById('setupFishType').value.trim();
        const fishQuantity = document.getElementById('setupFishQuantity').value;
        const startDate = document.getElementById('setupStartDate').value;
        const endDate = document.getElementById('setupEndDate').value;
        const phone = document.getElementById('setupPhone').value.trim();
        const purpose = document.getElementById('setupPurpose').value.trim();

        // Validation
        if (!userName || !fishType || !fishQuantity || !startDate || !endDate) {
          alert('กรุณากรอกข้อมูลที่จำเป็นให้ครบ');
          return;
        }

        if (new Date(endDate) < new Date(startDate)) {
          alert('วันสิ้นสุดต้องไม่น้อยกว่าวันเริ่มต้น');
          return;
        }

        this.disabled = true;
        this.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>กำลังบันทึก...';

        try {
          const res = await fetch('/admin/pond/<%= pond.id %>/setup', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              user_name: userName,
              fish_type: fishType,
              fish_quantity: parseInt(fishQuantity),
              start_date: startDate,
              end_date: endDate,
              phone: phone,
              purpose: purpose
            })
          });

          const data = await res.json();

          if (res.ok && data.success) {
            alert('ตั้งค่าการใช้งานบ่อเรียบร้อย');
            location.reload();
          } else {
            alert('เกิดข้อผิดพลาด: ' + (data.error || 'ไม่สามารถตั้งค่าได้'));
          }
        } catch (err) {
          alert('เกิดข้อผิดพลาด: ' + err.message);
        } finally {
          this.disabled = false;
          this.innerHTML = '<i class="bi bi-check-circle me-2"></i>ยืนยันการตั้งค่า';
        }
      });
    }
  </script>
</body>
</html>
