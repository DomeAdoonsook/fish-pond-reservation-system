<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - ระบบงานฟาร์ม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
    }
    .hero-section {
      padding: 60px 0;
      text-align: center;
    }
    .hero-logo {
      width: 120px;
      height: 120px;
      margin-bottom: 20px;
    }
    .hero-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: #2c3e50;
      margin-bottom: 10px;
    }
    .hero-subtitle {
      font-size: 1.1rem;
      color: #7f8c8d;
      margin-bottom: 40px;
    }
    .menu-card {
      background: white;
      border-radius: 20px;
      padding: 40px 30px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
      height: 100%;
      border: none;
    }
    .menu-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 60px rgba(0,0,0,0.15);
    }
    .menu-icon {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 25px;
      font-size: 3rem;
      color: white;
    }
    .menu-icon.pond { background: linear-gradient(135deg, #3498db, #2980b9); }
    .menu-icon.equipment { background: linear-gradient(135deg, #f39c12, #e67e22); }
    .menu-icon.stock { background: linear-gradient(135deg, #27ae60, #16a085); }
    .menu-icon.broadcast { background: linear-gradient(135deg, #9b59b6, #8e44ad); }

    .menu-title {
      font-size: 1.5rem;
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 15px;
    }
    .menu-desc {
      color: #7f8c8d;
      font-size: 0.95rem;
      margin-bottom: 25px;
      line-height: 1.6;
    }
    .menu-btn {
      padding: 12px 35px;
      border-radius: 50px;
      font-weight: 500;
      font-size: 1rem;
      transition: all 0.3s;
    }
    .menu-btn.pond { background: linear-gradient(135deg, #3498db, #2980b9); border: none; }
    .menu-btn.pond:hover { background: linear-gradient(135deg, #2980b9, #1f6dad); transform: scale(1.05); }
    .menu-btn.equipment { background: linear-gradient(135deg, #f39c12, #e67e22); border: none; }
    .menu-btn.equipment:hover { background: linear-gradient(135deg, #e67e22, #d35400); transform: scale(1.05); }
    .menu-btn.stock { background: linear-gradient(135deg, #27ae60, #16a085); border: none; }
    .menu-btn.stock:hover { background: linear-gradient(135deg, #16a085, #138d75); transform: scale(1.05); }
    .menu-btn.broadcast { background: linear-gradient(135deg, #9b59b6, #8e44ad); border: none; }
    .menu-btn.broadcast:hover { background: linear-gradient(135deg, #8e44ad, #7d3c98); transform: scale(1.05); }

    .badge-pending {
      position: absolute;
      top: 15px;
      right: 15px;
      font-size: 0.85rem;
      padding: 8px 12px;
      border-radius: 20px;
    }

    .stats-row {
      margin-top: 50px;
    }
    .stat-card {
      background: white;
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    }
    .stat-number {
      font-size: 2rem;
      font-weight: 700;
    }
    .stat-label {
      color: #7f8c8d;
      font-size: 0.9rem;
    }

    .footer-admin {
      text-align: center;
      padding: 30px;
      color: #7f8c8d;
      font-size: 0.9rem;
    }
    .admin-badge {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: #2c3e50;
      color: white;
      padding: 10px 20px;
      border-radius: 50px;
      font-size: 0.9rem;
      box-shadow: 0 5px 20px rgba(0,0,0,0.2);
      z-index: 1000;
      text-decoration: none;
    }
    .admin-badge:hover {
      background: #1a252f;
      color: white;
    }

    /* LINE Quota Bar - Compact */
    .line-quota-bar {
      background: linear-gradient(135deg, #00B900, #00C300);
      border-radius: 8px;
      padding: 8px 15px;
      color: white;
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 0.85rem;
      box-shadow: 0 2px 8px rgba(0, 185, 0, 0.2);
    }
    .line-quota-bar svg {
      width: 18px;
      height: 18px;
      margin-right: 8px;
    }
    .line-quota-bar .quota-info {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .line-quota-bar .quota-text {
      font-weight: 500;
    }
    .line-quota-bar .quota-numbers {
      display: flex;
      align-items: center;
      gap: 5px;
    }
    .line-quota-bar .quota-used {
      font-weight: 700;
    }
    .line-quota-bar .quota-progress-mini {
      width: 80px;
      height: 6px;
      background: rgba(255,255,255,0.3);
      border-radius: 3px;
      overflow: hidden;
    }
    .line-quota-bar .quota-progress-fill {
      height: 100%;
      border-radius: 3px;
      background: #fff;
    }
    .line-quota-bar .quota-progress-fill.warning { background: #ffc107; }
    .line-quota-bar .quota-progress-fill.danger { background: #dc3545; }
  </style>
</head>
<body>
  <div class="container">
    <!-- Hero Section -->
    <div class="hero-section">
      <img src="/images/logo.png" alt="Logo" class="hero-logo" onerror="this.style.display='none'">
      <h1 class="hero-title"><i class="bi bi-gear-wide-connected me-2"></i>ระบบงานฟาร์ม</h1>
      <p class="hero-subtitle">คณะเทคโนโลยีการประมงและทรัพยากรทางน้ำ มหาวิทยาลัยแม่โจ้</p>
    </div>

    <!-- LINE Quota Bar (Compact) -->
    <% if (typeof lineQuota !== 'undefined' && !lineQuota.error) { %>
    <%
      const usedPercent = lineQuota.limit > 0 ? (lineQuota.used / lineQuota.limit * 100) : 0;
      let progressClass = '';
      if (usedPercent >= 80) progressClass = 'danger';
      else if (usedPercent >= 50) progressClass = 'warning';
    %>
    <div class="row justify-content-center mb-3">
      <div class="col-md-6 col-lg-5">
        <div class="line-quota-bar">
          <div class="quota-info">
            <svg viewBox="0 0 24 24" fill="white">
              <path d="M19.365 9.863c.349 0 .63.285.63.631 0 .345-.281.63-.63.63H17.61v1.125h1.755c.349 0 .63.283.63.63 0 .344-.281.629-.63.629h-2.386c-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.627-.63h2.386c.349 0 .63.285.63.63 0 .349-.281.63-.63.63H17.61v1.125h1.755zm-3.855 3.016c0 .27-.174.51-.432.596-.064.021-.133.031-.199.031-.211 0-.391-.09-.51-.25l-2.443-3.317v2.94c0 .344-.279.629-.631.629-.346 0-.626-.285-.626-.629V8.108c0-.27.173-.51.43-.595.06-.023.136-.033.194-.033.195 0 .375.104.495.254l2.462 3.33V8.108c0-.345.282-.63.63-.63.345 0 .63.285.63.63v4.771zm-5.741 0c0 .344-.282.629-.631.629-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.627-.63.349 0 .631.285.631.63v4.771zm-2.466.629H4.917c-.345 0-.63-.285-.63-.629V8.108c0-.345.285-.63.63-.63.348 0 .63.285.63.63v4.141h1.756c.348 0 .629.283.629.63 0 .344-.281.629-.629.629M24 10.314C24 4.943 18.615.572 12 .572S0 4.943 0 10.314c0 4.811 4.27 8.842 10.035 9.608.391.082.923.258 1.058.59.12.301.079.766.038 1.08l-.164 1.02c-.045.301-.24 1.186 1.049.645 1.291-.539 6.916-4.078 9.436-6.975C23.176 14.393 24 12.458 24 10.314"/>
            </svg>
            <span class="quota-text">โควต้า</span>
          </div>
          <div class="quota-numbers">
            <span class="quota-used"><%= lineQuota.used %></span>/<%= lineQuota.limit %>
            <div class="quota-progress-mini">
              <div class="quota-progress-fill <%= progressClass %>" style="width: <%= usedPercent.toFixed(0) %>%"></div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <% } %>

    <!-- Menu Cards -->
    <div class="row g-4 justify-content-center">
      <!-- บ่อเลี้ยงปลา -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (pendingCount > 0) { %>
            <span class="badge bg-danger badge-pending"><%= pendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon pond">
            <i class="bi bi-droplet-half"></i>
          </div>
          <h3 class="menu-title">บ่อเลี้ยงปลา</h3>
          <p class="menu-desc">
            ดูผังบ่อ จัดการการจอง<br>
            อนุมัติคำขอใช้บ่อ
          </p>
          <a href="/admin/ponds" class="btn btn-primary menu-btn pond text-white">
            <i class="bi bi-grid-3x3-gap me-2"></i>เข้าสู่ระบบบ่อ
          </a>
        </div>
      </div>

      <!-- ยืม-คืน อุปกรณ์ -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (typeof equipmentPendingCount !== 'undefined' && equipmentPendingCount > 0) { %>
            <span class="badge bg-warning text-dark badge-pending"><%= equipmentPendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon equipment">
            <i class="bi bi-tools"></i>
          </div>
          <h3 class="menu-title">การยืม-คืน อุปกรณ์</h3>
          <p class="menu-desc">
            จัดการอุปกรณ์ ตรวจสอบสถานะ<br>
            อนุมัติคำขอยืม-คืน
          </p>
          <a href="/admin/equipment" class="btn btn-warning menu-btn equipment text-white">
            <i class="bi bi-box-seam me-2"></i>เข้าสู่ระบบอุปกรณ์
          </a>
        </div>
      </div>

      <!-- Stock วัสดุเกษตร -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <% if (typeof stockPendingCount !== 'undefined' && stockPendingCount > 0) { %>
            <span class="badge bg-success badge-pending"><%= stockPendingCount %> รออนุมัติ</span>
          <% } %>
          <div class="menu-icon stock">
            <i class="bi bi-archive"></i>
          </div>
          <h3 class="menu-title">Stock วัสดุเกษตร</h3>
          <p class="menu-desc">
            ตรวจสอบสต็อก เบิกจ่ายวัสดุ<br>
            อนุมัติคำขอเบิก
          </p>
          <a href="/admin/stock" class="btn btn-success menu-btn stock text-white">
            <i class="bi bi-clipboard-data me-2"></i>เข้าสู่ระบบสต็อก
          </a>
        </div>
      </div>

      <!-- Broadcast ประกาศข่าวสาร -->
      <div class="col-md-6 col-lg-4">
        <div class="menu-card position-relative">
          <div class="menu-icon broadcast">
            <i class="bi bi-megaphone"></i>
          </div>
          <h3 class="menu-title">ประกาศข่าวสาร</h3>
          <p class="menu-desc">
            ส่งข้อความถึงผู้ใช้ทุกคน<br>
            แจ้งข่าว ประกาศสำคัญ
          </p>
          <a href="/admin/broadcast" class="btn menu-btn broadcast text-white">
            <i class="bi bi-send me-2"></i>ส่งประกาศ
          </a>
        </div>
      </div>
    </div>

    <!-- Stats Row -->
    <div class="row g-3 stats-row justify-content-center">
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-primary"><%= status.available %></div>
          <div class="stat-label">บ่อว่าง</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-danger"><%= status.occupied %></div>
          <div class="stat-label">บ่อใช้งาน</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-warning"><%= status.pending %></div>
          <div class="stat-label">รออนุมัติ</div>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="stat-card">
          <div class="stat-number text-secondary"><%= status.maintenance %></div>
          <div class="stat-label">ปิดปรับปรุง</div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer-admin">
      ระบบงานฟาร์ม - คณะเทคโนโลยีการประมงและทรัพยากรทางน้ำ มหาวิทยาลัยแม่โจ้
    </div>
  </div>

  <!-- Admin Badge -->
  <% if (isLoggedIn && admin) { %>
  <a href="/admin/logout" class="admin-badge">
    <i class="bi bi-person-circle me-2"></i><%= admin.name || 'Admin' %> | ออกจากระบบ
  </a>
  <% } else { %>
  <a href="/admin/login" class="admin-badge">
    <i class="bi bi-box-arrow-in-right me-2"></i>เข้าสู่ระบบ Admin
  </a>
  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
